{
  "name": "sjaakluthart.nl",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "autoprefixer": "^7.1.2",
    "buildbranch": "^2.0.0",
    "postcss-clean": "^1.0.3",
    "postcss-cli": "^4.1.0"
  },
  "dependencies": {},
  "scripts": {
    "postcss": "postcss src/css/*.css -d build/css -u autoprefixer postcss-clean --no-map",
    "deploy": "buildbranch gh-pages build"
  }
}
