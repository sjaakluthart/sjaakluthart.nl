{
  "name": "sjaakluthart.nl",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "autoprefixer": "^9.6.1",
    "buildbranch": "^2.0.0",
    "postcss-clean": "^1.0.3",
    "postcss-cli": "^6.1.3"
  },
  "dependencies": {},
  "scripts": {
    "postcss": "postcss src/css/*.css -d build/css -u autoprefixer postcss-clean --no-map",
    "deploy": "buildbranch gh-pages build sjaakluthart.nl"
  }
}
